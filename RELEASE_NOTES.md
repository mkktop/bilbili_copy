## 更新内容

### ✨ 新增：我的关注列表

- 个人主页新增「我的关注」Tab，列出当前账号关注的全部 UP 主（头像/名字/签名，支持分页加载更多）。
- 点击关注的 UP 主 → 进入其投稿/合集主页（复用已有的 UP 主主页视图），可继续浏览投稿、合集并下载。
- 后端新增 `get_followings` command（`/x/relation/followings`，cookie 鉴权，无需 WBI 签名）。

### 🎨 界面
- 窗口标题栏从「B站视频下载」改为「BilbliCopy」。

## 安装/升级
- Windows: 下载 `.msi` 或 `.exe` 安装包，覆盖安装即可
- 应用内自动更新将在后台推送
