## 更新内容

### 🎉 新增

#### 暂停 / 恢复下载
- 下载中可点 **⏸ 暂停**，已下载部分保留，**▶ 恢复** 时从断点继续，不再从头重下。
- 暂停 / 取消按钮按任务状态智能切换：排队中（置顶 / 取消）、下载中（暂停 / 取消）、已暂停（恢复 / 取消）、失败（重试 / 删除）、完成（删除）。

#### 下载排队与并发控制
- 新增**下载调度器**：任务提交后进入优先级队列，按全局并发上限依次执行，避免无限制并发触发风控。
- 任务提交立即返回，前端通过事件感知进度 / 完成 / 失败，不再阻塞。

#### 优先级置顶
- 排队中的任务可点 **⏫ 置顶**，调度器优先取出执行（同优先级 FIFO）。

#### 分片级断点续传（首次暂停也能续传）
- 大文件按 **16MB 上限**切分并行下载（并发度不变，仍由设置里的并行线程数控制）。
- 每个分片下载成功后**立即持久化完成状态**到 `.meta`（互斥保护），暂停中断后恢复**只重下未完成的分片**。
- 续传时进度条直接显示在断点位置（如已下 80% 则从 80% 继续涨），不再有"从 0 开始"的错觉。
- 安全回退：换画质 / 编码（指纹不匹配）、CDN 返回大小变化、分片数变化（改了线程数）、临时文件丢失等场景，自动从头下载，不会损坏文件。

### 🔧 改进

- **崩溃恢复**：应用异常退出后，残留的下载中任务标记为**已暂停**（而非失败），用户可在下载列表手动恢复，最大程度保留进度。
- **跨会话续传指纹改造**：流指纹从基于会过期的 CDN URL 改为基于稳定属性（`bvid / cid / 画质 / 编码`），应用重启后重新请求流地址，只要画质 / 编码没变就能续传。
- **数据库 schema 升级到 v3**：`download_history` 新增 `quality`（画质，用于跨会话续传指纹匹配）和 `video_title`（合集名，用于恢复时重建下载目录）两列。老库自动迁移，数据不丢失。

### 🛡️ 稳健性（保留既有）

- 全局 / 单视频 / 流内三层并发控制、坏 CDN 节点黑名单（10 分钟 TTL）、多 URL 回退、指数退避重试、ffmpeg 合并降级、登录过期自动刷新重试、风控验证码流程 —— 全部保留。

### 🧪 测试

- Rust 单元测试增至 **34 项**，新增覆盖：分片数 16MB 上限计算、并行续传位图解析、位图 + 无临时文件回退、损坏 `.meta` 容错、单线程 / 并行模式切换、调度器优先级排序与去重、DB v3 迁移。

## 升级说明
- **自动升级**：现有 `data.db` 会在首次启动时自动从 v2 迁移到 v3（添加 `quality` / `video_title` 列），无需手动操作。
- Windows: 下载 `.msi` 或 `.exe` 安装包覆盖安装，应用内自动更新将在后台推送。
