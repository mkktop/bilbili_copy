## 更新内容

### 🐛 修复：视频详情返回后丢失来源页状态

- **修复返回退回空白页**：从「发现页 → UP 主投稿 → 视频详情」返回时，不再清空到空搜索页，而是停留在原来的 UP 主投稿列表（含已加载的更多分页）。
- **修复收藏夹返回丢状态**：从「个人主页 → 收藏夹 → 视频详情」返回时，停留在收藏夹视频列表，不再退回收藏夹目录。
- 同理修复：稍后再看、我的订阅、搜索结果进详情后返回的状态保留。

### 🔧 实现细节
- 详情页从独占视图改为固定定位覆盖层（`fixed inset-0`），来源页组件在详情态下保持挂载（被遮挡但不卸载），返回时其 state 完整保留。

## 安装/升级
- Windows: 下载 `.msi` 或 `.exe` 安装包，覆盖安装即可
- 应用内自动更新将在后台推送
