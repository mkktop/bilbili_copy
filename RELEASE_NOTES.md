## 更新内容

### ✨ 新增

#### 视频搜索高级筛选
- 发现页搜索视频时，新增**排序 / 时长 / 分区**三个筛选维度（分段按钮组形式，点击即生效）：
  - **排序**：综合 / 最新 / 播放量 / 弹幕数。
  - **时长**：全部 / <10分钟 / 10-30分钟 / 30-60分钟 / >60分钟。
  - **分区**：全分区 / 动画 / 番剧 / 音乐 / 舞蹈 / 游戏 / 科技 / 科普 / 生活 / 美食 / 鬼畜 / 时尚 / 娱乐 / 影视。
- **智能显示**：筛选器仅在「视频」搜索类型下显示；切换到 UP主 / 番剧 / 影视时自动隐藏（这三类搜索不支持这些参数），切回视频时保留上次选择。
- **翻页保留**：加载更多结果时筛选条件自动带上，不会回到综合排序。

### 🔧 改进

- **搜索后端参数化**：`search_videos` 命令新增 `order` / `duration` / `tids` 三个可选参数，B站搜索接口的排序 / 时长 / 分区能力从「硬编码默认值」改为「前端可控」。WBI 签名接口分支补传了之前缺失的 `tids` 参数。
- **请求竞态控制**：搜索请求加入 `searchReqId` 递增标记，快速切换筛选器时丢弃过期响应，避免结果错乱 / 重复累加。

### 🛡️ 稳健性（保留既有）

- 全局 / 单视频 / 流内三层并发控制、分片级断点续传、坏 CDN 节点黑名单、多 URL 回退、指数退避重试、ffmpeg 合并降级、登录过期自动刷新重试、风控验证码流程 —— 全部保留。

## 升级说明

- 本版本未改动数据库 schema，升级无数据迁移。
- 搜索筛选参数仅影响新发起的搜索，无持久化需求。
- Windows: 下载 `.msi` 或 `.exe` 安装包覆盖安装，应用内自动更新将在后台推送。
