## 更新内容

### ✨ 新功能
- **弹幕下载**：视频下载完成后自动保存弹幕为 ASS 字幕文件，可直接在播放器中加载显示
  - 采用 Lane 车道布局引擎，自动碰撞检测避免弹幕重叠
  - 支持滚动弹幕、顶部弹幕、底部弹幕、逆向弹幕
  - 3 次重试 + 线性退避，确保弹幕获取稳定性
- **字幕下载**：自动保存 CC 字幕为 SRT 文件（按语言分别保存，如 zh-Hans.srt、en.srt）
  - 自动过滤 AI 生成的字幕
  - WBI 签名 + 自动重试
- **设置页新增开关**：在"下载"设置页可独立开启/关闭弹幕和字幕下载

## 安装/升级
- Windows: 下载 `.msi` 或 `.exe` 安装包，覆盖安装即可
- 应用内自动更新将在后台推送
