## v0.7.5 更新内容

### ✨ 新增

#### 系统托盘最小化（关闭窗口后台继续下载）
- **关闭即最小化到托盘**：点击窗口关闭按钮不再直接退出，而是最小化到系统托盘，程序在后台继续运行
- **后台下载不中断**：下载调度器是独立后台任务，窗口隐藏后所有进行中 / 排队中的下载任务照常执行
- **托盘菜单**：右键托盘图标可「显示主窗口」或「退出」（真正退出进程）
- **点击托盘恢复**：左键单击 / 双击托盘图标即可恢复主窗口
- **首次最小化提示**：第一次最小化到托盘时弹一次系统通知（Windows 原生），告知「程序在后台运行、下载不会中断」，之后不再打扰

#### 可配置开关
- **设置 → 通用 → 窗口与托盘**：新增「关闭时最小化到托盘」开关，默认开启
- 关闭此开关后，点击关闭按钮恢复为传统行为（直接退出应用），进行中的下载会被中断

### 🔧 实现亮点

- **零后台逻辑改动**：下载调度器 `run_dispatcher` 本就是独立的 `tauri::async_runtime::spawn` 后台任务，与窗口生命周期解耦，窗口只要隐藏（而非销毁）下载自然继续
- **Tauri 2 原生托盘 API**：启用 `tauri` 的 `tray-icon` feature，复用 `icons/icon.ico` 作为托盘图标，无需新增图标资源
- **关闭拦截**：在 `on_window_event` 中捕获 `CloseRequested`，根据 `close_to_tray` 设置决定 `prevent_close()` + 隐藏，或放行退出；托盘「退出」走 `app.exit(0)` 绕过拦截实现真正退出
- **首次提示持久化**：`settings.json` 新增 `tray_hint_shown` 字段记录是否已提示过，原子写入防止丢失
- **新增 `tauri-plugin-notification`**：用于首次托盘提示的系统通知

### 📝 说明

- 本次未改动数据库 schema，升级无数据迁移
- `settings.json` 新增字段 `close_to_tray`（默认 `true`）和 `tray_hint_shown`（默认 `false`），旧版本配置自动迁移，默认开启托盘最小化
- Windows: 下载 `.msi` 或 `.exe` 安装包覆盖安装，应用内自动更新将在后台推送
