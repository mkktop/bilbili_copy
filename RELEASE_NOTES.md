## 更新内容

### 🎉 新增

#### 弹幕可视化配置
- 设置 → 下载页新增**弹幕渲染**卡片（仅在开启「下载弹幕」时显示）：
  - **字号**：小 / 中 / 大 三档（对应 ASS Fontsize 18 / 25 / 36）。
  - **滚动速度**：快 / 标准 / 慢 三档（滚动时长 8 / 15 / 25 秒，越大越慢）。
  - **透明度**：低 / 中 / 高 三档（显示透明度 0.2 / 0.5 / 0.8）。
- 新增**屏蔽区域**开关：可单独屏蔽**顶部弹幕**和**底部弹幕**，屏蔽后对应类型不会写入 ASS 文件。

#### 顶部 / 底部弹幕正确渲染
- 之前所有弹幕都被当作滚动弹幕处理，顶部 / 底部类型实际显示位置错误。
- 现在顶部弹幕用 `\pos` 固定显示在屏幕顶部、底部弹幕固定在底部（字幕位），各自独立通道碰撞，不再与滚动弹幕混排。

#### 字幕 VTT 导出
- 字幕除原有 **SRT** 外，新增 **VTT (WebVTT)** 格式选项（适合网页播放器与部分现代播放器）。
- 在设置 → 下载页选择导出格式（仅在开启「下载字幕」时显示），下载后按语言分别保存为 `xxx.zh-Hans.srt` / `xxx.zh-Hans.vtt`。

#### 字幕库模式（单任务，只下字幕不下视频）
- 视频详情页底部新增 **「仅下载字幕」** 勾选框，勾选后点击下载将跳过视频 / 音频下载，只抓取字幕（构建本地字幕库）。
- 字幕库模式下仍可附加弹幕（沿用全局「下载弹幕」开关）。
- 完成后照常进入下载列表，目标目录只有字幕文件（无 `.mp4`）。

### 🔧 改进

- **配置向后兼容**：弹幕渲染 / 字幕格式字段在 `settings.json` 中均带默认值，旧配置文件读取自动补全，无需迁移。
- `DanmakuOption` 由私有改为公开，`fetch_danmaku_ass` 接受外部注入的渲染配置，弹幕下载逻辑与全局设置解耦。

### 🛡️ 稳健性（保留既有）

- 全局 / 单视频 / 流内三层并发控制、分片级断点续传、坏 CDN 节点黑名单、多 URL 回退、指数退避重试、ffmpeg 合并降级、登录过期自动刷新重试、风控验证码流程 —— 全部保留。

### 🧪 测试

- Rust 单元测试新增覆盖：弹幕透明度 → ASS alpha 换算、滚动弹幕输出 `\move`、顶部 / 底部弹幕输出 `\pos`、屏蔽开关丢弃对应类型弹幕、字号 / 透明度反映到 ASS 样式、SRT 用逗号分隔毫秒、VTT 以 `WEBVTT` 开头并用点分隔。

## 升级说明

- 本版本未改动数据库 schema（仍为 v3），升级无数据迁移。
- Windows: 下载 `.msi` 或 `.exe` 安装包覆盖安装，应用内自动更新将在后台推送。
