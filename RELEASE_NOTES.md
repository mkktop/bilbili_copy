## 更新内容

### ✨ 新增：内容发现与批量下载（5 大 API）

本次更新补齐了对比 bili-sync-up 后发现的缺失 API，从"只能粘贴 URL 解析"扩展到完整的**内容发现 + 批量下载入口**。所有列表点击均可走原有解析 → 详情 → 下载链路。

#### 稍后再看（Watch Later）
- 个人主页新增「稍后再看」Tab，列出 B 站稍后再看列表，一键解析下载。

#### UP 主投稿列表（Submission）
- 发现页搜索 UP 主 → 进入 UP 主主页，浏览其全部投稿（支持分页、WBI 签名）。
- 同时展示 UP 主卡片信息（头像、名字、签名）。

#### UGC 合集 / 系列（Collection）
- UP 主主页「合集/列表」Tab，浏览 UP 主创建的视频合集（season）与系列（series），点击合集查看内部视频。
- 此前仅支持番剧 season，现补齐普通 UP 主制作的视频合集。

#### 搜索（Search）
- 顶部新增「🔍发现」入口，支持搜索**视频 / UP 主 / 番剧 / 影视**四类。
- 搜索接口带三级回退（legacy → wbi → all/v2），提升稳定性。

#### 订阅的合集 / 收藏夹
- 个人主页新增「我的订阅」Tab，浏览自己订阅的收藏夹与合集（`fav/folder/collected/list`）。

### 🛠 技术细节
- 后端新增 4 个模块 + 8 个 Tauri command，复用现有 WBI 签名与凭证体系，新增代码 0 warning。
- 前端抽取通用 `VideoCard` 组件，4 个场景复用；新增发现页、订阅 Tab 等组件。

## 安装/升级
- Windows: 下载 `.msi` 或 `.exe` 安装包，覆盖安装即可
- 应用内自动更新将在后台推送
