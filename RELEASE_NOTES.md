## 更新内容

### 🔄 Cookie 自动刷新
- 应用启动时自动检测 Cookie 有效期，临近过期自动刷新
- 下载/解析过程中遇到登录过期（-101）自动刷新后重试
- 完整实现 B站 3 步刷新协议（RSA 加密 → CSRF 提取 → 换新 Cookie → 确认）

### 🛡️ 风控/验证码处理
- 检测下载风控（v_voucher），自动弹出 GeeTest v3 滑块验证码
- 验证通过后自动缓存 gaia_vtoken（1 小时有效），后续请求自动携带
- 支持注册验证码 + 提交验证结果完整流程

### 🔊 音频默认音质调整
- 默认最高音质从 192K 调整为 Hi-Res 无损

---

## 📥 安装

1. 下载下方 `.msi` 文件
2. 双击运行，按提示安装

## 🔄 升级

- **自动更新**：已安装旧版本的用户，启动应用后会自动检测新版本，点击「立即更新」即可
- **手动更新**：下载最新 `.msi` 重新安装，覆盖旧版本
