## v1.2.0 更新内容

功能版：历史弹幕、进度条缩略图预览、下载库管理，并修复一个影响所有互动操作的接口问题。

### ⚠️ 重要修复：点赞 / 投币 / 收藏 / 稍后再看实际不生效
- 真实账号冒烟测试发现：互动类 POST 请求缺少 `Origin` 头时，B站返回成功但**静默不执行**——v1.1.0 里点赞、投币、收藏、加入稍后再看都受影响
- 本版已修复并逐项真机验证（加 → 列表确认 → 删，全链路生效）

### 🔥 历史弹幕
- 新增设置项「历史弹幕天数」（设置 → 字幕弹幕，0/3/7/14/30 天，默认关闭）
- 开启后：下载 ASS 弹幕与播放器在线弹幕都会额外合并最近 N 天的历史弹幕（按北京时间自然日，自动去重）
- 逐日请求带 350ms 间隔防风控；单日失败不影响其余天数

### 🖼️ 进度条缩略图预览
- 播放器进度条区域悬停鼠标，显示对应时间点的画面预览（官方 videoshot 雪碧图）与时间标签
- 无索引的视频自动隐藏预览，不影响播放

### 📚 下载库管理
- 下载列表新增状态筛选：全部 / 下载中 / 已完成 / 已暂停 / 失败
- 「已完成」视图按合集名自动分组展示
- 新增「删除记录和文件」：一键清理视频 + 弹幕/字幕/NFO/封面等附属文件（前缀防误删：不会碰到标题相近的其它视频）

### 🧪 质量保障
- 新增真实登录态冒烟测试工具（`cargo run --example smoke_api`）：覆盖视频信息、AI 总结、相关推荐、评论/楼中楼、稍后再看增删、粉丝、入站必刷、搜索联想、历史弹幕、缩略图共 12 条链路，全部真机通过
- 单元测试 69 → 78 个（新增 videoshot 定位、按状态过滤、文件删除防误删等）

---

## v1.0.0 正式版

> 🎉 **BilbliCopy 1.0 正式发布** —— 基于 Tauri 2（Rust + React）构建的现代化 B站视频下载器，轻量、快速、功能完整。

### 📥 视频下载
- **最高 8K / HDR / 杜比视界画质**，音质支持 Hi-Res 无损与杜比全景声，编码优先级可自定义（AVC / HEVC / AV1）
- **多线程分片下载 + 断点续传**：中途暂停、退出重启都能接着下，跨会话续传由流指纹保护
- **批量下载**：多分P / 番剧全集 / 合集 / 收藏夹 / UP 主投稿，选中即下；支持仅音频（m4a/mp3）、仅字幕模式
- **下载队列管理**：并发数 / 限速可配，暂停 / 恢复 / 重试 / 优先级置顶，完成后系统通知
- **自定义文件名模板**：`{up}/{title} [{bvid}]` 等占位符自由组合，按 UP 主 / 合集自动归档
- **附加内容**：弹幕（ASS 渲染可配字号/速度/透明度）、字幕导出（SRT/VTT）、NFO 元数据刮削（Kodi / Jellyfin / Emby 可直接入库）

### ▶️ 内置在线播放器
- 无需下载即点即播，画质自由切换，精准拖动秒到（sidx 索引），弱网自动重试不断播
- 实时弹幕、多语言字幕（含 AI 字幕）、倍速 0.5x–2x、分P 选集
- **播放列表连播**：跨稿件自动切下一集，配合每周必看一键连播整期
- 进度记忆续播、迷你小窗、系统画中画、全套键盘快捷键
