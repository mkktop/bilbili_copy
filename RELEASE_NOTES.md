## 更新内容

### 🎉 新增

#### 下载历史封面
- 下载列表现在显示视频封面缩略图（与解析列表视觉统一），老记录（无封面）回退显示文件图标。
- 数据库 schema 自动迁移到 **v2**：`download_history` 新增 `pic` 列。首次启动自动升级，老数据不丢失。

#### 分页页码跳转
- 解析历史 / 下载历史列表从「上一页/下一页」升级为**可点击页码按钮组**（如 `‹ [1] 2 3 … 8 ›`），支持直达任意页码，无需逐页翻。
- 新增可复用的 `<Pagination>` 组件，页码超过 7 页自动折叠省略号。

#### 全局 Toast 通知
- 自建极简 toast 系统（零依赖）：解析失败、下载失败、清除历史成功/失败、风控触发等操作均有顶部浮层反馈。
- 后端错误自动映射为**中文友好文案**（网络超时、未登录、Cookie 失效、BVID 无效、数据库锁等），不再显示英文堆栈。

### 🔧 修复（状态一致性）

- **详情返回保持页码**：之前从解析历史第 N 页点进详情再返回，会被强制踢回第 1 页；现在保留在原页码。
- **删除后处理空页**：删除当前页最后一条记录时，自动回退到上一页，不再卡在空白页。
- **解析成功不强制刷新整页**：浏览第 N 页时解析新视频，不再打断当前浏览（仅在停留在第 1 页时才刷新列表）。
- **静默错误全部补齐**：UP 主主页（投稿/合集/合集视频）、收藏夹视频、订阅视频等列表加载失败时，之前只 `console.error` 用户无感知；现在统一显示错误态 + 重试按钮 + toast。

### ♻️ 重构

- **PAGE_SIZE 单一来源**：每页 20 条的常量从前端 3 处重复定义收敛到 `lib/constants.ts`（后端 `history.rs` 因前后端无法共享仍保留，靠注释约定）。
- **formatCount 去重**：播放量格式化函数从 4 处重复定义（且逻辑不一致）收敛到 `VideoCard.tsx` 单一来源，统一支持万/亿分级。
- **节流常量化**：下载进度落库的 1200ms 节流值从硬编码抽到 `DOWNLOAD_PROGRESS_THROTTLE_MS`。

### 🧪 测试

- 新增数据库迁移单元测试（5 项），覆盖：老库 v1→v2 升级、迁移幂等性、全新库建表、带封面记录读写、老记录（无封面）兼容读取。

## 升级说明
- **自动升级**：现有 `data.db` 会在首次启动时自动从 v1 迁移到 v2（添加 `pic` 列），无需手动操作。
- Windows: 下载 `.msi` 或 `.exe` 安装包覆盖安装，应用内自动更新将在后台推送。
