## v1.0.2 更新内容

一次全面的审计修复版本：安全加固 + 下载正确性 + 稳定性，共修复 40 余项问题。升级后数据库与登录凭证自动迁移，**解析/下载历史全部保留，无需任何操作**。

### 🛡️ 安全加固
- **登录凭证加密存储**：`credentials.json` 改用 Windows DPAPI 加密（旧明文首次启动自动升级），同机其它账户/直接读文件均拿不到 Cookie
- **修复专栏阅读页 XSS 缺口**：HTML 净化器补上属性转义，恶意文章无法再向 `img`/`a` 标签注入事件属性
- **内置流代理防凭据外带**：biliproxy 仅对 `*.bilibili.com` 附带 Cookie，并拒绝本地/内网地址

### 🐛 下载正确性（重要）
- **修复断点续传拼接损坏**：重试/换 CDN 时重新计算已下载大小，续传必须收到 206，下载结束校验字节数——不再产出「能播但内容重复/截断」的坏文件
- **修复限速只有设定值 1/8**：限速 1024 KB/s 现在就是 1024 KB/s
- **修复并发分P信号量永不清理**（v1.0.1 残留）：调整「单视频分P并发数」后立即生效，不再活到重启
- **取消任务现在清理临时文件**：与「暂停保留续传」明确区分，不再残留数 GB `.tmp`
- **ffmpeg 输出原子化**：合并中途失败不再留下半截「成品」文件，也不静默覆盖已有视频
- **修复重启后排队任务变「幽灵任务」**：崩溃残留的 queued/downloading 一律恢复为「已暂停」，可手动续传
- 并发容量热调不再瞬时超限；取消请求不再落空；分片长度精确校验

### 🔁 恢复 / 重试
- **恢复/重试保持原画质与下载模式**：仅字幕/仅音频任务重试不再变成下载完整视频；跨会话续传指纹恢复匹配（数据库自动迁移 v100→v101，历史保留）

### ⚙️ 其它修复（节选）
- 登录状态全局同步（详情页点赞/投币不再误报"请先登录"）
- 观看历史翻页不再重复/漏项（完整游标回传）
- 画质/音质兜底尊重用户上下限，不再静默拿最高画质
- WBI 签名密钥缓存缩短至 2h + `~` 字符签名修复；风控/登录过期等硬错误不再连发约 28 次无效请求
- GPU 指纹厂商名跟随预设（AMD/Intel 卡不再带 NVIDIA 字样）
- NFO 简介防 XML 注入、时间改北京时间
- 清空历史/取消下载加确认弹窗；设置保存失败可见
- 弹幕 ASS 防 override 标签注入；评论加载防竞态重复；播放器拉流 15s 超时不再永久卡加载

---

## v1.0.0 正式版

> 🎉 **BilbliCopy 1.0 正式发布** —— 基于 Tauri 2（Rust + React）构建的现代化 B站视频下载器，轻量、快速、功能完整。

### 📥 视频下载
- **最高 8K / HDR / 杜比视界画质**，音质支持 Hi-Res 无损与杜比全景声，编码优先级可自定义（AVC / HEVC / AV1）
- **多线程分片下载 + 断点续传**：中途暂停、退出重启都能接着下，跨会话续传由流指纹保护
- **批量下载**：多分P / 番剧全集 / 合集 / 收藏夹 / UP 主投稿，选中即下；支持仅音频（m4a/mp3）、仅字幕模式
- **下载队列管理**：并发数 / 限速可配，暂停 / 恢复 / 重试 / 优先级置顶，完成后系统通知
- **自定义文件名模板**：`{up}/{title} [{bvid}]` 等占位符自由组合，按 UP 主 / 合集自动归档
- **附加内容**：弹幕（ASS 渲染可配字号/速度/透明度）、字幕导出（SRT/VTT）、NFO 元数据刮削（Kodi / Jellyfin / Emby 可直接入库）

### ▶️ 内置在线播放器
- 无需下载即点即播，画质自由切换，精准拖动秒到（sidx 索引），弱网自动重试不断播
- 实时弹幕、多语言字幕（含 AI 字幕）、倍速 0.5x–2x、分P 选集
- **播放列表连播**：跨稿件自动切下一集，配合每周必看一键连播整期
- 进度记忆续播、迷你小窗、系统画中画、全套键盘快捷键

### ⚠️ 旧版用户升级须知（0.x → 1.0）
- 1.0 与 0.x 数据结构不兼容，升级后首次启动会自动重建数据库，本地解析记录 / 下载历史 / 播放进度将被重置
- 已下载的视频文件、登录状态、常规设置不受影响
