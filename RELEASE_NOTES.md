## v0.6.5 更新内容

### ✨ 新增

#### 观看历史（个人主页新 Tab）
- 在「个人主页」新增「观看历史」Tab，需登录后查看
- API: `https://api.bilibili.com/x/web-interface/history/cursor`，cursor 分页
- 卡片显示已观看进度（badge：「已看 65%」/「已看完」）
- 翻页用 cursor 游标（`view_at` 参数），首页不传
- 「加载更多」按钮支持查看更早历史

#### 热门排行榜（导航栏新入口）
- 顶部导航栏新增 🏆 按钮，独立的「热门排行榜」视图
- API: `https://api.bilibili.com/x/web-interface/ranking/v2`
- 分区筛选（16 个一级分区：全站/动画/音乐/游戏/娱乐/科技/科普/生活/美食/舞蹈/时尚/动物/汽车/运动/鬼畜/影视）
- 名次徽章（金/银/铜配色，Top 3 突出）
- 卡片显示综合分（v2 接口不返回该字段时自动隐藏）
- 切换分区显示语义说明：全站榜 vs 分区榜是两套独立算法

### 🔧 改进

- **新增 TID_OPTIONS 共享常量**（`src/lib/constants.ts`）：搜索筛选 / 排行榜筛选共用同一份分区列表
- 排行榜与搜索列表去除「番剧(13)」（该 tid 在 UGC 排行榜返回 -400，且 PGC 番剧搜索走 media_bangumi 类型不依赖 tid）
- 排行榜新增「动物 / 汽车 / 运动」三个主站有的分区（之前漏了）

### 🐛 修复

- **观看历史 cursor 参数错误**：原代码误用 `max` 参数（含义为「时间上限限定」），B站返回空 list；改为按文档正确的 `view_at` 翻页游标，立即恢复正常数据
- 观看历史放宽过滤：原仅保留 `business == "archive"`，现保留所有有 bvid 的记录（番剧 PGC 等也能进）
- `has_more` 判断更准确：原要求「返回条数 == 页大小」才算有更多（首页/末页不足一页会误判），改为「cursor.view_at > 0 即可能有更多」

### 🛡 健壮性（保留既有）

- 全局 / 单视频 / 流内三层并发控制、分片级续传、坏 CDN 节点黑名单、长 URL 绕避、指数退避重试、ffmpeg 合并降级、登录过期自动刷新重试、验证码流程 —— 全部保留

## 升级说明

- 本次未改动数据库 schema，升级无数据迁移
- 观看历史 / 排行榜均为实时 API，不落库
- Windows: 下载 `.msi` 或 `.exe` 安装包覆盖安装，应用内自动更新将在后台推送
