## 更新内容

### 🐛 重要修复（代码全量审计）

- **修复并发下载限制失效**：此前当已有下载在运行时，新下载会绕过"最大并发数"设置全部同时执行，可能触发 B站风控。现已严格按设置限流。
- **修复网络抖动导致登录丢失**：此前启动时遇到一次网络超时/解析失败，已保存的登录就会被删除、被迫重新扫码。现仅当账号确实失效（-101/-104）才清除，临时网络问题一律保留登录。
- **修复并发数设为 0 时下载永久卡死**：并发上限现在强制 ≥1，避免零许可信号量导致下载挂起。
- **修复断点续传可能产生损坏文件**：换画质/编码重新下载同一视频时，旧实现会把新流拼到旧流后面。现已校验流指纹，不符则从头下载。

### 🔒 安全与稳定性
- **弹幕接口改用 HTTPS**：避免在不可信网络下 Cookie 被明文截获。
- **字幕文件名清洗**：防止语言代码含特殊字符时写到下载目录之外。
- **全局请求超时**：所有 B站 API 调用增加超时，避免单个挂起连接永久阻塞任务。
- **日志膨胀治理**：文件日志改为 INFO 级别，避免长时间运行后日志文件无限增大。
- **锁中毒恢复**：并发锁在中毒（异常 panic）后可自动恢复，不再连锁导致全部下载失败。

### ⚡ 体验优化
- **进度落库节流**：下载进度写库改为每 1.2 秒一次，降低 SQLite 锁竞争与界面卡顿。
- **登录弹窗轮询稳定**：后台有下载在跑时，扫码登录不再因界面重渲染而反复重置。
- **验证码弹窗资源清理**：GeeTest 验证码实例在关闭时正确销毁，避免内存/节点泄漏。

## 安装/升级
- Windows: 下载 `.msi` 或 `.exe` 安装包，覆盖安装即可
- 应用内自动更新将在后台推送
